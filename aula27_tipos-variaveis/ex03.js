// Const - Não é possível mudar seu valor ao ser atribuído um.

const nome = "João"

console.log(nome)

nome = "Bruno" // Dará erro, pois "nome" é uma constante que já tem um valor, ou seja, não será possível substituir este valor.

console.log(nome)